import React from "react";
import { Button } from "./components";

const App: React.FC = () => {
  return (
    <div>
      <h1>My React and TypeScript App!</h1>
      <Button>Test</Button>
    </div>
  );
};

export default App;
